export const patterns = {

  // ----------------------------------------------------
  // 🔹 1. EMAIL (RFC 5322 compliant, enterprise safe)
  // ----------------------------------------------------
  email: /^[a-zA-Z0-9._%+-]+@[a-zA-Z0-9.-]+\.[A-Za-z]{2,}$/,
  

  // ----------------------------------------------------
  // 🔹 2. STRONG PASSWORD (enterprise level)
  // must contain: Uppercase + lowercase + number + symbol
  // min length 8
  // ----------------------------------------------------
  strongPassword:
    /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?#&])[A-Za-z\d@$!%*?#&]{8,}$/,
    
  password: /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$/,
  
  // ----------------------------------------------------
  // 🔹 3. NAME (first/last name, only letters & spaces)
  // No numbers or symbols
  // ----------------------------------------------------
  name: /^[A-Za-z]+(?: [A-Za-z]+)*$/,


  // ----------------------------------------------------
  // 🔹 4. PHONE NUMBER (international format)
  // Allows +91, +1, 10-digit, etc.
  // ----------------------------------------------------
  phone: /^\+?[0-9]{10,14}$/,


  // ----------------------------------------------------
  // 🔹 5. INDIAN PHONE NUMBER STRICT (10 digits only)
  // ----------------------------------------------------
  phoneIndia: /^[6-9]\d{9}$/,


  // ----------------------------------------------------
  // 🔹 6. ZIP / PIN CODE (India)
  // ----------------------------------------------------
  pinCode: /^[1-9][0-9]{5}$/,


  // ----------------------------------------------------
  // 🔹 7. DATE (YYYY-MM-DD)
  // ----------------------------------------------------
  date: /^\d{4}-\d{2}-\d{2}$/,


  // ----------------------------------------------------
  // 🔹 8. AMOUNT / MONEY (up to 2 decimals)
  // ----------------------------------------------------
  amount: /^\d+(\.\d{1,2})?$/,


  // ----------------------------------------------------
  // 🔹 9. USERNAME (letters, numbers, underscore)
  // 3-16 characters
  // ----------------------------------------------------
  username: /^[a-zA-Z0-9_]{3,16}$/,


  // ----------------------------------------------------
  // 🔹 10. ADDRESS (letters, numbers, ',', '-', space)
  // ----------------------------------------------------
  address: /^[A-Za-z0-9\s,'-]{5,200}$/,


  // ----------------------------------------------------
  // 🔹 11. URL (enterprise-safe)
  // ----------------------------------------------------
  url: /^(https?:\/\/)?([\w\-]+\.)+[\w\-]{2,}(\/[\w\-._~:/?#[\]@!$&'()*+,;=]*)?$/,


  // ----------------------------------------------------
  // 🔹 12. PAN Number (India)
  // Format: ABCDE1234F
  // ----------------------------------------------------
  pan: /^[A-Z]{5}[0-9]{4}[A-Z]$/,


  // ----------------------------------------------------
  // 🔹 13. Aadhaar Number (India — 12 digits)
  // ----------------------------------------------------
  aadhaar: /^[0-9]{12}$/,


  // ----------------------------------------------------
  // 🔹 14. GST Number (India)
  // ----------------------------------------------------
  gst: /^[0-9]{2}[A-Z]{5}[0-9]{4}[A-Z]{1}[1-9A-Z]{1}Z[0-9A-Z]{1}$/,


  // ----------------------------------------------------
  // 🔹 15. VEHICLE NUMBER (India)
  // MH12AB1234
  // ----------------------------------------------------
  vehicle: /^[A-Z]{2}[0-9]{2}[A-Z]{1,2}[0-9]{4}$/,


  // ----------------------------------------------------
  // 🔹 16. OTP (4 to 8 digits)
  // ----------------------------------------------------
  otp: /^[0-9]{4,8}$/,
 
  //   ⭐ What this blocks:
  // ❌ 32-10-2000
  // ❌ 00-05-1999
  // ❌ 31-02-2001
  // ❌ 29-02-2001 (not leap year)
  // ❌ 15/13/2020

  // ⭐ What this allows:
  // ✔ 25-12-1998
  // ✔ 01/01/2000
  // ✔ 29/02/2004 (valid leap year)
  dob: /^(19|20)\d\d-(0[1-9]|1[0-2])-(0[1-9]|[12]\d|3[01])$/,
  country:/^[A-Za-z ]+$/,
  id:/^[a-zA-Z0-9]{1,10}$/,
  Number:/^[0-9]+$/

};
